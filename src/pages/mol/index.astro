---
import MOLLayout from "@/layouts/MOLLayout.astro";

// Default entry when visiting /gol/ (index). Change this to whichever
// entry you want to show by default.
const selectedEntryId = "scarface";
---

<MOLLayout selectedEntryId={selectedEntryId} />
