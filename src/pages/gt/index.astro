---
import GTLayout from "@/layouts/GTLayout.astro";

// Default entry when visiting /gol/ (index). Change this to whichever
// entry you want to show by default.
const selectedEntryId = "gta1";
---

<GTLayout selectedEntryId={selectedEntryId} />
