---
import { defaultAssets } from ".";
---

<div class="hidden">
	{
		Object.values(defaultAssets).map((asset) => (
			<img
				src={asset.src}
				alt="preload"
				loading="eager"
				fetchpriority="high"
			/>
		))
	}
</div>
